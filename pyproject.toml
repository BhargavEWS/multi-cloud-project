[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["app/api/tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["infra"]

[tool.ruff.lint]
select = ["E", "F", "I"]
